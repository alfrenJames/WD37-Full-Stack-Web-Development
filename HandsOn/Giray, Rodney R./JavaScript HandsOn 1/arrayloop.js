let browser = [];
browser.push('Brave', 'Firefox', 'Chrome', 'Opera', 'IE');

console.log(browser);

for(index = 0; index < browser.length; index++)
{
    console.log('index:', index, browser[index]);
}